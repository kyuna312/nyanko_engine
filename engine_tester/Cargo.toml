[package]
name = "engine_tester"
version = "0.1.0"
edition = "2021"

[dependencies]
nyanko_engine = { path = "../" }  # Make sure this path correctly points to nyanko_engine
gl = "0.14.0"
glfw = "0.58.0"
cgmath = "0.18.0"
